/**
 * Идентификатор трека стека.
 *
 * Скраб находит трек через `document.getElementById`, а не через ref и не
 * строкой-селектором. Ref родительского контейнера присваивается уже после
 * layout-эффектов дочернего About и на первом проходе пуст, а строку GSAP
 * разрешил бы через селектор контекста `useGSAP` — то есть искал бы трек
 * внутри самой секции, хотя трек её родитель.
 */
export const STACK_ID = 'about';

/**
 * Запас прокрутки между залипшим About и наезжающим Services, svh.
 *
 * Пока трек стека протягивается на эту величину, About стоит на месте, текст
 * манифеста проявляется, а Services ещё не коснулся кадра. Ровно на этой
 * отметке нижняя секция входит снизу.
 */
export const STACK_SCRUB_SVH = 70;

/**
 * Доля запаса, к которой скраб текста уже завершён.
 * Остаток — пауза между «текст готов» и «Services пошёл вверх».
 */
export const TEXT_SETTLED_AT = 0.8;

/**
 * Отступ залипшей секции от верха кадра, px.
 * Держать в паре с классом `lg:top-[15px]` на секции About.
 */
export const STACK_STICKY_TOP_PX = 15;

/**
 * Насколько раньше залипания стартует проявление текста, в долях кадра.
 *
 * Это позиция верха трека в момент старта: 0.6 — секция заняла нижние 60%
 * экрана, манифест только показался снизу, слова начинают проявляться уже
 * на подъезде. 0 означало бы старт ровно в момент залипания.
 *
 * На конечную точку не влияет: она рассчитывается от момента залипания,
 * поэтому пауза перед наездом Services остаётся прежней.
 */
export const TEXT_LEAD_IN = 0.6;
